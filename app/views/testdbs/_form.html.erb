<%= form_with(model: testdb, local: true) do |form| %>
  <% if testdb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testdb.errors.count, "error") %> prohibited this testdb from being saved:</h2>

      <ul>
      <% testdb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>

  <div class="field">
    <%= form.label :yes %>
    <%= form.check_box :yes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
